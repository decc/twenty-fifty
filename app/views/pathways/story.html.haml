= render 'tabbed_menu'
#submenu

%script{ :type => 'text/javascript' }
  $('#menu_story').addClass('active')
  twentyfifty.longDescriptions = #{raw ModelStructure.instance.long_descriptions.to_json};
  twentyfifty.setup(twentyfifty.Story)

#stories
  #demand_story.story
  #supply_story.story
  #ghg_story.story
.clear