%script{ :type => 'text/javascript' }
  twentyfifty.setup(twentyfifty.CostsSensitivity);

#results.costssensitivity
  %ul#comparatorchoice.dropdown
    %li
      = link_to_function raw("Choose comparison" + image_tag('dropdown-arrow.png')), "$('ul#view_comparatorchoice').toggle()"
      %ul#view_comparatorchoice.choices
        - saved_pathways.each do |name,code|
          %li= link_to_function name, "twentyfifty.switchCompator('#{code}');$('ul#view_comparatorchoice').toggle()"
          
  %h1
    The cost of your pathway compared with another, allowing simple variation in cost estimates.

  = render 'cost_caveats'

  #costssensitivity
    
  = render 'essential_notes'    
    
= render 'classic_controls'
